import { Card, CardContent, CardHeader, CardTitle, CardFooter } from "ui/card";

export const MovieCard = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle></CardTitle>
      </CardHeader>
      <CardContent>

      </CardContent>
      <CardFooter>

      </CardFooter>
    </Card>
  )
}
